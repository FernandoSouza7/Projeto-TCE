import { FilteNamePipe } from './filte-name.pipe';

describe('FilteNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilteNamePipe();
    expect(pipe).toBeTruthy();
  });
});
